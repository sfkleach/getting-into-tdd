package uk.org.codehub.getting_into_tdd;

/*
This is a bonus problem that builds on top of scramble.py. Given some
scrambled text (the 'ciphertext') and the original secret, return the
original plaintext.
*/

class Unscramble {
	
	/**
	 * Given a previously scrambled message, recover the original text using
	 * the secret used to scramble it.
	 * 
	 * @param secret A 26-letter string that is a permutation of the alphabet (lower case).
	 * @param ciphertext A scrambled message.
	 * @return The original plaintext.
	 */
	public static String unscramble( String secret, String ciphertext ) {
		return null;
	}
    
}